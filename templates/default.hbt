{{> html-header}}
{{> header}}
<main class="container-fluid">
	<div class="row">
		<div class="col-sm-5 center-block">
			{{#if title}}
				<h1>{{title}}</h1>
			{{/if}}
			{{{ contents }}}
		</div>
	</div>
</main>
{{> footer}}
{{> html-footer}}